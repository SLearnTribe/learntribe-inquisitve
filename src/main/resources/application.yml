
server:
  port: 8089
spring:
  application:
    name: learntribe-inquisitive
  datasource:
    url: jdbc:postgresql://localhost:5032/inquisitive
    password: password
    username: keycloak
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          keycloak:
            authorization-grant-type: client_credentials
            client-id: cloud-gateway-client
            client-secret: 8CwDfo6yxWAUQuSz5j6QZHP3ElhfvcOQ
        provider:
          keycloak:
            token-uri: http://localhost:8085/realms/master/protocol/openid-connect/token
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8085/realms/master
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

feign:
  client:
    config:
      default:
        loggerLevel: full
logging:
  level:
    com:
      smilebat:
        learntribe:
          learntribeclients: DEBUG


